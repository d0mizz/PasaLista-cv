{
  "name": "pasa-lista",
  "version": "1.0.0",
  "private": true,
  "description": "PasaLista static web served with Node for Cloud Run",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "seed": "node seed.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}

